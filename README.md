# Random Lunch Agent

A simple lunch recommendation app that randomly selects a nearby lunch spot based on user preferences.

## Setup

1. Clone the repo.
2. Install dependencies:

```bash
npm install
